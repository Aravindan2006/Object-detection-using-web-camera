# Object-detection-using-web-camera

## Project Overview

This project performs real-time object detection using a web camera and the YOLO (You Only Look Once) deep learning model.
It uses OpenCV’s DNN module to detect objects such as people, cars, bottles, chairs, etc. in live video feed.

## Features

Real-time object detection using YOLO

Automatically detects and labels multiple objects

Shows bounding boxes + confidence score

Press C to capture image

Press Q to quit

Works with any USB or laptop webcam
